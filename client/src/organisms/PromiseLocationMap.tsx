import styled from "styled-components";

const PromiseLocationMap = () => {
  return <Map id="map"></Map>;
};

export default PromiseLocationMap;

const Map = styled.div`
  width: 75vw;
  height: 30vh;
`;
